# 掼蛋游戏需求分析

- 版本：v0.5
- 状态：已确认关键决策（万能牌方案 / 交付形态 / AI 难度 / 辅助功能 / 手机 APP 端策略：Android 优先、直接分发 APK/PWA、手机端优先开发；托管与后端：Vercel + Supabase）
- 参考规则：[“掼蛋”规则及入门讲解（美篇）](https://www.meipian.cn/517ncq2i)、[淮安市智力运动会掼蛋竞赛规则（云比赛）](https://www.yunbisai.com/tpl/news/NewsDetail-4119.html)

---

## 1. 项目概述

### 1.1 背景与目标
掼蛋（淮安掼蛋）是流行于江苏及全国的四人两副牌升级类棋牌游戏。本项目目标是开发一款**可实际运行、规则可配置、体验完整**的掼蛋游戏。经需求确认，首期以**网页版本地同屏（4 人轮流操作同一台设备，任一座位可填充 AI）**为最小可行产品（MVP）。**手机端为第一目标环境**（触屏、竖屏优先，网页版为过渡形态），后续通过 Capacitor 打包为 **Android APP（直接分发 APK/PWA，不上商店）**，再扩展在线联机。

### 1.2 成功标准
- 一局完整对局（含进贡/还贡/抗贡/升级/过 A）可在浏览器中流畅跑通
- 规则引擎与界面解耦，核心规则有单元测试覆盖
- 桌面端与移动端浏览器均可正常操作

## 2. 用户与使用场景

| 场景 | 描述 | 优先级 |
|---|---|---|
| 本地同屏 | 4 人轮流操作同一设备（手机/平板聚会），支持防偷看模式 | P0 |
| 人机练习 | 1 名玩家 + 3 个 AI 对手（3 档难度可选），随时开局 | P1 |
| 手机 APP | 同一套代码打包为 Android/iOS 应用（Capacitor），可上架 | P3 |
| 在线联机 | 创建/加入房间，与好友对战 | P4 |
| 观战/回放 | 对局记录回放 | P4 |

目标用户：熟悉或想学习掼蛋规则的玩家，界面语言为中文。

第一设备：手机（竖屏）与平板，桌面浏览器为次要适配目标。

## 3. 核心游戏规则需求

> 掼蛋存在"官方竞赛规则"与"民间主流规则"的差异。本需求以**可配置规则引擎**方式实现，默认采用**民间主流规则（红桃逢人配）**，关键差异点见 3.7 配置项表。

### 3.1 牌与座位
- 两副标准扑克共 **108 张**（含大小王各 2 张），4 名玩家各发 **27 张**
- 玩家交叉入座，**对家为队友**，两两成队
- 出牌顺序：逆时针轮转（可配置为顺时针）

### 3.2 级牌与万能牌（逢人配）
- 级数：2 → 3 → … → K → A，共 **13 级**，从 2 打起
- **级牌**：本局主打的点数，共 4 张。点数大小：大王 > 小王 > **级牌** > A > K > … > 3 > 2（打 2 时 2 为级牌，仍大于 A）
- **逢人配（万能牌）**：**4 张级牌均为万能牌**（经确认采用官方竞赛规则），可替代除大小王以外的任意牌参与组牌（顺子、连对、钢板、炸弹均可）；单出或作对子时按级牌点数计算
- 备选规则（可配置）：民间主流玩法仅**红桃级牌 2 张**作万能牌

### 3.3 牌型定义与大小比较

| 牌型 | 组成 | 大小比较 |
|---|---|---|
| 单张 | 1 张 | 点数大者胜 |
| 对子 | 2 张同点数 | 点数大者胜 |
| 三张（三不带） | 3 张同点数 | 点数大者胜 |
| 三带二 | 三张 + 一对 | 先比三张部分点数 |
| 顺子（杂色顺） | 5 张连续点数 | 同长度顺子比最大牌；A 可作顶端（10JQKA）或当 1（A2345，可配置） |
| 连对（姊妹对） | 3 对及以上连续对子 | 同对数比最大对 |
| 钢板（三顺） | 2 组及以上连续三张 | 同组数比最大组 |
| 同花顺 | 5 张同花色连续牌 | 属于炸弹，见下 |
| 炸弹 | 4~8 张同点数 | 张数多者大，同张数比点数 |
| 天王炸 | 4 张王牌 | 最大 |

**炸弹层级**：天王炸 > 8 张炸 > 7 张炸 > 6 张炸 > **同花顺** > 5 张炸 > 4 张炸（同花顺位置可配置）

- 炸弹可压任何非炸弹牌型；同类牌型必须**张数/长度相同**才能互压（如 6 张顺只能压 6 张顺）
- 掼蛋不存在"三带一""四带二""飞机带翅膀"等牌型

### 3.4 出牌流程
1. 首局：洗牌后翻一张数字牌确定首家（可配置为随机）
2. 后续局：按 3.5 贡牌规则确定首出者
3. 玩家依次跟牌：出**同牌型且更大**的牌，或出炸弹/天王炸，或选择"过"
4. 一家出牌后其余三家均"过"，该家获得牌权，自由出任意牌型
5. **接风**：某玩家打出最后一手牌且无人能压时，出牌权交给其**同伴**
6. 名次：手牌出完即按序得名次（头游 → 二游 → 三游 → 末游），前三家出完即结束
7. 报牌：剩余 ≤10 张时主动报牌（可配置开关）；有出牌权者可"问牌"

### 3.5 进贡 / 还贡 / 抗贡（第二局起）

| 情形 | 规则 |
|---|---|
| 单下 | 末游向头游进贡手中**最大牌**（红桃逢人配不可贡），头游还一张 ≤10 的牌；还贡后**末游先出** |
| 双下 | 末游贡头游、三游贡二游，各还一张；**进贡牌大者先出**，一样大随机 |
| 抗贡 | 进贡方抓到 2 张大王（单人 2 张，或双下双方各 1 张）则免贡，**头游先出** |
| 还贡限制 | 民间规则：还贡 ≤10；竞赛规则：还同伴 ≤10、还对方任意（可配置） |

### 3.6 胜负与升级（1.2.3 升级法）

| 头游队友名次 | 升级 |
|---|---|
| 二游（双上） | 升 3 级 |
| 三游 | 升 2 级 |
| 末游 | 升 1 级 |

- 升级即级数 +N（如打 5 时双上 → 打 8）
- **过 A**：打到 A 后须头游且**同伴非末游**（同伴二游/三游）才算获胜；失败则继续打 A（部分地区规则：连续 3 局同伴末游则退回 2，可配置）
- 可选积分制（炸弹计数翻倍等，可配置；MVP 不实现）

### 3.7 规则配置项汇总（引擎需参数化）

| 配置项 | 默认值 | 备选 |
|---|---|---|
| 万能牌方案 | **全级牌万能（4 张，官方竞赛，已确认）** | 红桃逢人配（2 张） |
| 顺子长度 | 固定 5 张 | 5 张及以上（竞赛规则） |
| 顺子含 2 / A2345 | 允许 A2345 | 禁止 |
| 连对对数上限 | 3 对 | 不限 |
| 钢板组数上限 | 2 组 | 不限 |
| 同花顺位置 | 6 张炸与 5 张炸之间 | 其他 |
| 还贡上限 | ≤10 | 任意 |
| 过 A 失败 | 继续打 A | 连输 3 局退到 2 |
| 出牌方向 | 逆时针 | 顺时针 |

## 4. 功能需求

### 4.1 对局功能（P0）
- 洗牌发牌、座位分配、队伍标识
- **座位配置**：每座位可选「人类 / AI」，AI 可选三档难度，发牌前锁定
- **同屏操作**：当前操作者醒目提示、手牌明暗切换、防偷看遮罩（可配置）
- 手牌管理：自动排序（按点数/花色可切换）、选牌出牌、非法牌型校验与提示
- 出牌区与各家剩余张数展示
- 操作按钮：出牌 / 提示 / 不出 / 托管
- 出牌合法性校验、压牌判断（统一走规则引擎）
- 进贡 / 还贡 / 抗贡流程交互
- 结算界面：名次、升级情况、当前级数、胜负判定
- 全新开局 / 下一局

### 4.2 辅助功能（P0，首期包含，已确认）
- **提示**：按当前牌型给出可出的最小/最优牌
- **托管**：AI 接管操作（超时自动托管，可配置）
- **记牌器**：统计已出牌与剩余牌（可配置开关）
- **AI 对手**：3 档难度（简单=随机合法出牌；中等=贪心策略；困难=记牌+拆牌+炸弹时机）
- **对局日志**：显示各家出牌记录
- **历史战绩**：升级进程、局数、胜负统计（localStorage 持久化）

### 4.3 体验优化（P1，首期包含）
- 出牌动画、音效、亮牌反馈
- 响应式布局（桌面 / 平板 / 手机）
- 悔牌（出牌前确认，可配置）

### 4.4 在线功能（P4，预留）
- 房间系统、邀请码、快速匹配
- 聊天 / 表情
- 断线重连、防刷新丢局
- 服务端权威校验（防作弊）
- 账号体系与云存档（配合 APP 端）

## 5. 非功能需求

| 项 | 要求 |
|---|---|
| 性能 | 操作响应 <100ms；AI 思考 <2s/手；单局 <3 分钟 |
| 兼容性 | **手机端为第一目标环境**：主流 Android 浏览器/WebView 优先，最小宽度 360px；桌面浏览器其次 |
| 移动端触控 | 可点击目标 ≥44px；安全区适配（刘海屏）；竖屏锁定；防误触与误退 |
| 离线能力 | PWA 离线缓存，断网不丢当前对局 |
| 可靠性 | 本地进度可恢复（刷新不丢局） |
| 可维护性 | 规则引擎为纯函数模块，与 UI 解耦；关键规则有单元测试 |
| 可配置性 | 3.7 全部配置项通过配置对象驱动 |

## 6. 技术方案建议

- **技术栈**：TypeScript + Vite + 原生 DOM/Canvas（后续可换 Phaser），不依赖重型框架
- **界面策略**：移动优先（mobile-first）响应式，竖屏单列牌桌布局，触控操作为第一交互方式
- **架构分层**：
  - `rules/` 规则引擎（纯函数）：牌型识别、大小比较、合法出牌枚举、提示算法
  - `state/` 对局状态机：阶段（发牌 → 贡牌 → 出牌 → 结算）与回合流转
  - `ai/` 三个难度级别的出牌策略
  - `ui/` 界面组件：牌桌、手牌区、出牌区、控件、结算
- **数据模型**：Card（点数/花色/是否万能）、Hand、Trick、Round、Match
- **存储**：localStorage（战绩、配置）
- **在线化扩展**（M5）：优先采用 Vercel + Supabase 方案（见 6.2）；若需国内部署，退化为 Node.js + WebSocket 自建服务器，规则引擎仍复用

### 6.1 手机 APP 端方案（Capacitor，推荐）

| 路线 | 说明 | 代码复用 | 成本 |
|---|---|---|---|
| H5 + PWA | 响应式网页 + 离线缓存，浏览器"添加到主屏幕" | 100% | 最低（非商店 App，原生能力受限） |
| **Capacitor 混合 App（推荐）** | 同一套 Web 代码包成原生壳，可上架 Android/iOS | 90%+ | 低（免费开源，需原生构建环境） |
| Flutter / RN 重写 | UI 用原生框架重写，体验最原生 | 仅规则引擎可复用 | 高（UI 全部重做） |

**选择 Capacitor 的理由**：与"规则引擎纯函数 + UI 解耦"架构完全契合——规则引擎、状态机、AI 在 Web / App / 未来服务端三端通用；Vite 构建产物直接由 Capacitor 加壳（`npx cap add android/ios`），无需重写业务代码。

**打包与上架要点**：
- Android：需 Android Studio（Java SDK），产物 APK/AAB；iOS：需 macOS + Xcode + Apple 开发者账号（$99/年）
- **发布方式（已确认）**：直接分发 APK / PWA 链接，暂不上商店（规避软著与 APP 备案流程）；后续如需上架国内安卓商店再补资质
- 在线联机后需要账号体系与云存档时，通过 Capacitor 原生插件 + 后端实现（推送、支付同理）

### 6.2 托管与后端方案（Vercel + Supabase）

| 阶段 | 方案 | 说明 |
|---|---|---|
| MVP（本地同屏） | **Vercel 托管静态页** | 无需后端；战绩/配置存 localStorage；Vercel Hobby 免费额度足够，顺带提供 PWA 链接供手机预览 |
| 在线联机（M5） | **Supabase + Vercel** | Auth（登录/游客）、Realtime（房间与出牌同步）、Edge Functions（服务端仲裁）、Postgres（房间/战绩/对局记录） |

**契合点**：规则引擎为纯 TypeScript，可直接运行在 Supabase Edge Functions（Deno）与 Vercel Functions 中——前后端复用同一套规则校验代码，天然支持防作弊（服务端权威）。

**注意点**：
- 回合制对延迟要求低（秒级），Supabase Realtime（WebSocket 广播）足够
- 免费额度：Vercel Hobby（100GB 带宽/月）、Supabase Free（500MB 数据库）对初期够用
- **国内访问风险**：Vercel/Supabase 为境外服务，国内直连可能慢或不稳（`*.vercel.app` 常被限制）；若在线对战目标用户主要在国内，需评估国内云（腾讯/阿里静态托管 + 自建/国内 BaaS）；本地同屏与 APK 玩法不受影响
- 手机 APP（Capacitor）与 Supabase 集成无障碍（WebView 内同域 API + JWT）

## 7. 里程碑

| 阶段 | 内容 | 交付物 |
|---|---|---|
| M0 | 需求确认（本文档评审） | 定稿的需求文档 |
| M1 | 规则引擎 + 单元测试 | 纯 TS 模块，用例覆盖全部牌型与配置项 |
| M2 | 本地同屏可玩（手机浏览器/PWA 优先，桌面其次） | 手机可玩完整对局 |
| M3 | 体验完善 | 提示 / 托管 / 记牌器 / 动画音效 / 战绩（移动端优先验收） |
| M4（可选） | 手机 APP：Capacitor 打包 Android（直接分发 APK） | 可安装的 APK |
| M5（可选） | 在线联机（Vercel + Supabase） | 房间 + 匹配 + 断线重连 |

## 8. 验收标准

- [ ] 108 张牌、27 张/人、4 人对家成队
- [ ] 全部牌型可正确识别与比较（含万能级牌参与的组合）
- [ ] 进贡 / 还贡 / 抗贡 / 接风 / 升级 / 过 A 全流程可走通
- [ ] 非法出牌被拦截并给出原因
- [ ] 4 人同屏轮流操作正常；任一座位填 AI 时可完成一局（≤3 分钟）
- [ ] 规则配置项全部生效
- [ ] 提示 / 托管 / 记牌器 / 音效动画 / 战绩存档可用
- [ ] 手机浏览器 / PWA 与桌面端操作正常（**手机为第一验收环境**）

## 9. 风险与开放问题

| 风险 / 问题 | 影响 | 对策 |
|---|---|---|
| 规则版本繁多（万能牌方案已确认；顺子长度、连对/钢板上限、过 A 细节等） | 实现与玩家预期不符 | 全部配置化（默认竞赛口径）；开局显示"当前规则" |
| 炸弹 / 万能牌组合枚举量大 | 牌型判断易出错 | 穷举单元测试，与主流 App 行为对拍 |
| 同屏模式防偷看 | 公平性体验 | 明暗切换 + 遮罩模式（可配置） |
| AI 难度难调 | 体验不佳 | 分档策略 + 实测调参 |
| 在线化反作弊 | 公平性 | 服务端仲裁（P4） |
| APP 上架资质（软著、备案） | 商店审核周期长 | 先发 APK/PWA，资质齐备后再上架 |
| iOS 构建环境（macOS/Xcode） | 无 Mac 无法出 iOS 包 | Android 优先；iOS 外包或后置 |
| 境外托管国内访问不稳（Vercel/Supabase） | 在线体验差 | MVP 不受影响；在线阶段按目标地区选托管（国内云备选） |
